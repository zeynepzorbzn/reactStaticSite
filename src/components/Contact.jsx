export const Contact = ({title,description}) => {
      return (
        <main>

       <h2>{title}</h2>
      <p>{description}</p>
      <button>Daha Fazla</button>
      

        </main>
      );
}